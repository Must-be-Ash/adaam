Commands

Only surf as tool + firecrawl and X API
Do we have coingecko? Can we use the free ones like Snapshot (needed for gov)

npm run type-check

npx tsc --noEmit

pkill -f "next dev" || true
lsof -ti:3000 | xargs kill -9 2>/dev/null || true

caffeinate -dims claude

/model claude-opus-4-6[1m]
/model claude-opus-4-8[1m]

rm -rf .next && npm run build && npx tsc --noEmit

npm run dev -- -p 3000

caffeinate -dims npm run dev

DEBUG=true pnpm dev   


pkill -f "vercel dev" && pkill -f "vite" || true

lsof -ti:8081
kill -9 49668 50565

rm -rf .next

ls -la

npm install --legacy-peer-deps

git pull origin main

git reset --hard HEAD

git add .
git commit -m " First commit "
git push origin main

git add .
git commit -m "  added og image  "
git push origin main

git add .
git commit -m "Last Working Version"
git push origin main


git checkout -b v8
git add .
git commit -m " branchestuuuuff "
git push origin v8  git checkout main

curl --request POST --url https://api.firecrawl.dev/v1/x402/search

curl --request POST --url https://api.cdp.coinbase.com/platform/v2/x402/discovery/resources

pnpm start --characters="characters/ChainSleuth-character.json"

tree -L 5 -I 'node_modules|.git'

npm run type-check 2>&1 | grep "error TS" | wc -l
npm run type-check 2>&1 | head -30

//////


cd cdp-langchain/examples/chatbot-typescript/web-ui && npm run dev

Space
&nbsp;
Small space
&thinsp;
‘
&apos;

python scraper.py
node page_crawler.js

vercel --prod

brew install tree
 tree -L 4 -I 'node_modules|.git'

tree -L 3

rm -rf .next
rm -rf node_modules/.cache


VERCEL_BLOB=vercel_blob_rw_HIso9Xmk1Y4NfYzd_8rlElR3zFRkNAmyuGt3bV6IglGZYWf

Circle
TEST_API_KEY:5bcda8e28eff4302042d9e01fda8fd63:33154c30419b8378af28ba61bab76b52

Ivebeenfuckinggettingit73!

Employee ID	211796
